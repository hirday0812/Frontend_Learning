import css from "./Display.module.css";

// The main code in App.jsx is broken down into components 
// Thats the main motive of react

function Display(){
  return <input type="text" className={css.display} />
}

export default Display;

